# Node / Electron
node_modules/
out/
dist/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Android
android/.gradle/
android/app/build/
android/build/
android/gradle-temp/
android/gradle.zip
android/local.properties
android/.idea/
*.apk
*.aab
*.keystore

# TTS model files (downloaded via npm run setup:tts)
resources/tts-model/

# Build output
release/

# Voice cloning workspace (OpenVoice, models, generated samples)
tmp/

# Source recordings and images (not for distribution)
Dinosaur Audio/
Dinosaur Images/

# Claude Code
.claude/

# Voice-clone reference recording. A real person's voice — deliberately NOT
# distributed. Keep your own copy here; the TTS engine clones it when present and
# silently falls back to the model's default voice when it is absent, so builds
# without it still work.
resources/voice/
